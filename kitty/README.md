Kitty conf file should reside:

- Ubuntu: `~/.config/kitty/kitty.conf`

Clone repo:

```bash
cd ~ && git clone https://github.com/gregg127/dotfiles
```

Create symlink:

```bash
ln -s ~/dotfiles/kitty/kitty.conf ~/.config/kitty/kitty.conf
```
